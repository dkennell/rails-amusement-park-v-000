<%= attraction.name %><br>
<% u = User.find_by(id: session[:user_id]) %>
<% if u.admin? %>
  <%= link_to "Show #{attraction.name}", "/attractions/#{attraction.id}" %><br>
<% else %>
<%= link_to "Go on #{attraction.name}", "/attractions/#{attraction.id}" %><br><br>
<% end %>